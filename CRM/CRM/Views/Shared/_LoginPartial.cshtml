@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li class="notifications">
                <a href="#">
                    <span class="glyphicon glyphicon-globe" id="globe"></span>
                    <span class="badge js-notification-count hide"></span>
                </a>

            </li>
            @*<li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">style<span class="style-caret">&#9660</span></a>
                <ul class="dropdown-menu" id="style-dropdown-menu">

                    <li>@Html.ActionLink("Dark", "SetStyle", "Style", new { id = 1 }, null)</li>
                    <li>@Html.ActionLink("Sky", "SetStyle", "Style", new { id = 2 }, null)</li>
                    <li>@Html.ActionLink("Default", "SetStyle", "Style", new { id = 3 }, null)</li>

                </ul>
            </li>*@

            <li class="dropdown">
                @*@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })*@

                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">Hello @(User.Identity.GetUserName() + "!")<span class="style-caret">&#9660</span></a>
                @*<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">style<span class="style-caret">&#9660</span></a>*@
                <ul class="dropdown-menu" id="style-dropdown-menu">

                    <li>@Html.ActionLink("Reset Password/Change Theme", "Index", "Manage")</li>
                    <li>@Html.ActionLink("My Group", "Index", "Group")</li>
                    <li>@Html.ActionLink("My Customers", "Index", "Customer")</li>
                    <li>@Html.ActionLink("My Tasks", "UserTasks", "Tasks")</li>
                    <li>@Html.ActionLink("My Messages", "UserMessages", "Messages")</li>
                    @if (Request.IsAuthenticated && User.IsInRole("Manager"))
                    {
                        <li>@Html.ActionLink("Join Requests", "Index", "JoinRequests")</li>
                    }
                </ul>
            </li>




            @*<li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>*@
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}
